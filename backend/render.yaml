services:
  - type: web
    name: video-downloader
    runtime: python
    buildCommand: sudo apt-get update && sudo apt-get install -y ffmpeg && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
